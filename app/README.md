# Veritas web app

Next.js 16 (App Router) frontend. See the [root README](../README.md) for
what this project does and how to run it end to end.

```bash
npm install
npm run dev      # http://localhost:3000
npm test         # Vitest unit tests
npm run test:e2e # Playwright, needs ml-service + Postgres running
```
